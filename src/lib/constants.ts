/** Nome do cookie HTTP-only que guarda o id da barbearia ativa. */
export const ACTIVE_SHOP_ID_COOKIE = "active_shop_id";

/** Idade máxima do cookie em segundos (1 ano). */
export const ACTIVE_SHOP_COOKIE_MAX_AGE = 60 * 60 * 24 * 365;
